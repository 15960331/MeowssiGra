'use client';

import { Button, Img } from '@chakra-ui/react';

export default function Home() {
  // TODO: load a folder with multiple png files
  const handleClick = () => {};

  return (
    <main>
      <Img
        src=""
        alt=""
      />
      <Button onClick={handleClick}>Load</Button>
    </main>
  );
}
